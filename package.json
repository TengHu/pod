{
  "name": "pod-build",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Build pipeline for pod skill templates",
  "scripts": {
    "gen:skill-docs": "bun run scripts/gen-skill-docs.ts",
    "gen:check": "bun run scripts/gen-skill-docs.ts --dry-run",
    "build": "bun run scripts/gen-skill-docs.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
